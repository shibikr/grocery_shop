<h2>Edit cart detail</h2>

<%= render "form.html", changeset: @changeset,
                        action: cart_detail_path(@conn, :update, @cart_detail) %>

<%= link "Back", to: cart_detail_path(@conn, :index) %>
